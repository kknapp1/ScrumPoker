{
  "name": "scrum-poker-backend",
  "version": "0.1.0",
  "private": true,
  "description": "Lambda handlers for Scrum Poker WebSocket API",
  "type": "commonjs",
  "scripts": {
    "test": "node --test handlers/*.test.js"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.0.0",
    "@aws-sdk/lib-dynamodb": "^3.0.0",
    "@aws-sdk/client-apigatewaymanagementapi": "^3.0.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.0"
  }
}

// Shared constants — keep in sync with frontend/src/constants.js
const MAX_PARTICIPANTS = 50

// Keep in sync with CARD_DECKS keys in frontend/src/constants.js
const VALID_DECK_KEYS = ['fibonacci', 'tshirt', 'powers']

module.exports = { MAX_PARTICIPANTS, VALID_DECK_KEYS }
